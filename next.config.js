/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      "sreena.oorjit.net",
      "cdn.findyourbmw.ae"
     
    ],
  },
}

module.exports = nextConfig
